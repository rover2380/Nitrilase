$m=0;@f="";$i=0;$j=1;@array="";$a = (ACDEFGHIKLMNPQRSTVWY);@f = split(//, $a);
$file =$ARGV[0];open(FP,"$file");while ($line=<FP>){    chomp($line);    if ($line =~/>/)    { # print "\n$line\n\n";    }    if($line !~/>/)     {    @array = split(//, $line); print "-1 "; $first=substr($line,0,25); @first=$first; @firs = split(//, $first);  for($m=0;$m<@f;$m++)  {     for($i=0;$i<@firs;$i++)     {     if($f[$m] eq "$firs[$i]")  {      $c++;  }     }     $l=$c*5;     print "$j:";
     printf"%4.4f ",$l;     #     printf"%3.2f ", $l;     $c=0;     $j++; } $last=$len-25; $middle=substr($line,25,$last); @middl = split(//, $middle);  if(@array>50) {     for($m=0;$m<@f;$m++)      {  for($i=0;$i<@middl;$i++)  {         if($f[$m] eq "$middl[$i]")      {   $c++;      }  }  $l=($c*100)/@middl;  print "$j:";  $j++;  printf"%4.4f ",$l; #  printf"%3.2f ", $l;  $c=0;     } } else {     for($i=0;$i<25;$i++)     {  print "$j:0.00 "; # print "0.0000 ";  $j++;     }      }     
 $end=substr($line,$last,25); @end=$end;    @en = split(//, $end); for($m=0;$m<@f;$m++)  {     for($i=0;$i<@en;$i++)     {  if($f[$m] eq "$en[$i]")  {      $c++;  }     }     $l=$c*5;     print "$j:";     $j++;     printf "%4.4f ",$l;#     printf"%3.2f ", $l;     $c=0;  }  print "\n"; $j=1;    }}
